from functions import*
import logging
#import Functions    moduleName.funtion  or moduleName.variable
#import Fuctions as f   so we can use f as a object of the module

## we have logging module ( import logging),
my_age()
logger = logging.getLogger("main logger")
logger.error("this will print out when error happend")